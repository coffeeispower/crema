package online.coffeeispower.crema.core.graphics

/**
 * Specifies how the border is positioned at the edges of objects.
 *
 * @see [online.coffeeispower.crema.core.graphics.renderer.FrameRecording.drawRectBorder]
 */
enum class BorderMode {
    Outside,
    Middle,
    Inside
}
